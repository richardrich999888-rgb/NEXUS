[package]
name = "autonomic-system"
version = "0.1.0"
edition = "2021"
description = "Autonomic nervous system for ASI - ACT/CALM mode regulation"
authors = ["NEXUS Team"]

[dependencies]
homeostasis-engine = { path = "../homeostasis-engine" }
thiserror = "1.0"
serde = { version = "1.0", features = ["derive"] }
tracing = "0.1"

[dev-dependencies]
