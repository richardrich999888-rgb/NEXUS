[package]
name = "developmental-gates"
version = "0.1.0"
edition = "2021"
description = "Developmental gating for staged capability unlock in ASI"
authors = ["NEXUS Team"]

[dependencies]
homeostasis-engine = { path = "../homeostasis-engine" }
thiserror = "1.0"
serde = { version = "1.0", features = ["derive"] }
tracing = "0.1"

[dev-dependencies]
